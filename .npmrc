# The plugin's peer deps (react, @deepseek-ai/dsh-*, @deepseek-ai/cordis) are
# supplied by the harness shell, not by npm. Don't let npm 7+ try to
# auto-install (or fail on) them when a contributor runs `npm install` for the
# dev-only test dependencies.
legacy-peer-deps=true
