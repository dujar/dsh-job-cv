{
  "// note": "Type-checking only — no emit. The plugin ships plain .js; this gates the host half (stores, routes, app services, MCP shell) against real type errors. The DSH client bundle (lib/client.js and its lib/client/ fragments) is one shared IIFE scope with no module boundaries and is linted, not type-checked.",
  "compilerOptions": {
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "target": "es2022",
    "lib": ["es2023"],
    "types": ["node"],
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "skipLibCheck": true,
    "strict": false,
    "noImplicitAny": false,
    "noImplicitThis": true,
    "noUncheckedIndexedAccess": false,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["lib/**/*.js", "bin/**/*.js"],
  "exclude": ["node_modules", "lib/client.js", "lib/client/**"]
}
